import curses
from utils import show_message, draw_border

def additional_package_managers_menu(stdscr):
    show_message(stdscr, "Zusätzliche Paketmanager verwalten (Flatpak, Snap, AppImage) coming soon!")